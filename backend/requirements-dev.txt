# Dev dependencies — install with: pip install -r requirements-dev.txt
# Includes everything in requirements.txt plus test tooling.
-r requirements.txt

# Testing
pytest==8.4.2
pytest-asyncio==0.24.0
httpx==0.28.1
aiosqlite==0.20.0
